# SQLite keeps the platform fully self-contained — no external DB needed.
DATABASE_URL="file:./dev.db"

# Secret used to HMAC-sign session cookies. Override in production.
SESSION_SECRET="ripplr-dev-secret-change-me"
